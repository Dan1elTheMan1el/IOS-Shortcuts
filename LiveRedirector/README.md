Automatically open any URL to LiveContainer!

# LiveRedirector (Userscript)

This is based on my app [LiveRedirector](https://github.com/Dan1elTheMan1el/LiveRedirect). Since iOS 26 broke sideloaded safari extensions, I made this version that doesn't require sideloading.

**NOTE: Requires [Userscripts App](https://apps.apple.com/ca/app/userscripts/id1463298887)**

## Features
- Automatically redirect specific URLs to LiveContainer app
- Adds floating button to matching sites to manually redirect
- Fully configurable using shortcut
- Select specific LiveContainer apps to open for each URL

## Setup instructions
- Identify your Userscripts directory
- Install the LiveRedirector shortcut and select the Userscripts folder during setup
- Use the shortcut to configure userscript settings

## Usage instructions
- Ensure Userscripts Safari extension is enabled
- Any matching URL will redirect to LiveContainer automatically! (Youtube, Instagram, etc.)

enjoy :)